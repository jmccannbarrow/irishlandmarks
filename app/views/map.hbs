{{> mainmenu _id="viewMapDetails"}}
<section class="ui raised segment">
    <div class="ui grid ">
        <div class="ui ten wide column">
            <div class="ui stacked fluid form segment">

                <h3 class="ui header">View Landmark Details</h3>

                <div class="field">
                    <style>
                        #map {
                            width: 100%;
                            height: 460px;
                            background-color: grey;
                        }
                    </style>
                    <h4>Map</h4>

                    <!--The div element for the map -->
                    <div id="map"></div>

                    <script>
                        // Initialize and add the map
                        function initMap() {
                            // The location
                            let location = {lat: {{landmark.latitude}}, lng: {{landmark.longitude}}, title: 'Start' };
                            //let trail_end = {lat: , lng: , title: 'End' };
                            // The map, centered
                            let map = new google.maps.Map(
                                    document.getElementById('map'), {zoom: 17, center: location, mapTypeId: 'hybrid'});
                            // The marker, positioned
                            let marker = new google.maps.Marker({position: location, map: map, title: "Start Back Strand"});
                            //let end_marker = new google.maps.Marker({position: trail_end, map: map, title: "End Back Strand"});

                            marker.setMap(map);
                        }
                    </script>
                    <!--Load the API from the specified URL
                    * The async attribute allows the browser to render the page while the API loads
                    * The key parameter will contain your own API key (which is not needed for this tutorial)
                    * The callback parameter executes the initMap() function
                    -->
                    <script async defer
                            src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDU3i8M25m-m2Fv-3dm_gum36ZYRZ0WQ90&callback=initMap">
                    </script>        </div>
            </div>



        </div>
</section>

